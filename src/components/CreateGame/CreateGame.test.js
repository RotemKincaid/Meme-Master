const createGame = require("./CreateGame.js")
// import io from "socket.io-client";
// const socket = io("http://localhost:4052");

describe("Create Game Container", () => {
  describe("send game", () => {
    

    // it("should emit the right game number", () => {
    //   const gamepin = 16231;
    //   socket.emit("1234", gamepin);

    //   socket.on("4321", data);
    //   expect(data).toEqual(16231);
    // });

    it("Should receive the game pin", () => {
      
    })







  });
});
